[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA: serve index.html for client-side routes (if you add routing later)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
